Packer Tools for Lint-Review
============================

Tools for building an AMI to run `lint-review` with packer.

```bash
export AWS_ACCESS_KEY=xxx
export AWS_ACCESS_SECRET_KEY=yyy

packer build packer.json
```
